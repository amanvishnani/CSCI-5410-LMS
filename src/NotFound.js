import React from "react";

const NotFound = () => {
  return (
    <h1 style={{ fontStyle: "italic" }}>
      The path specificed is not found in the application.
    </h1>
  );
};

export default NotFound;
